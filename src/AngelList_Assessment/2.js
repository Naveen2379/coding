/* what's wrong with the code */
const testString = "hello world";
const isAllCaps = false;
if(testString === testString.toUpperCase()) {
    isAllCaps = true;
}

/* const isAllCaps should be replaced with let */


function minTollCost(input) {
    let inputArray = input;
    console.log(inputArray);

    let minCost = inputArray[0];
    if(inputArray.length > 2) {
        for(let i=1; i<inputArray.length;i++) {

            if() {

            }
        }
    }
    else {
        return minCost;
    }

    return '';
}


minTollCost([5,10,3]);